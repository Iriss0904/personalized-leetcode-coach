export const publicAlgorithmHintLevels = [
  "Name the invariant and test it on a tiny input.",
  "Choose the data structure that makes the invariant cheap to maintain.",
  "Trace the state transition immediately before the first wrong output.",
] as const;
